/** Target bounding-sphere radius after auto-normalize on load. */
export const SCENE_TARGET_RADIUS = 1.5;

export const SCENE_GRID_SIZE = SCENE_TARGET_RADIUS * 4;
export const SCENE_GRID_DIVISIONS = 20;
export const SCENE_AXES_LENGTH = SCENE_TARGET_RADIUS * 1.25;
